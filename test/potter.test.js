describe("potter", function() {
    it("should create a new domain pot if none currently exists");

    it("should create a new context if none currently exists");

    it("should create a new translation if none currently exists");

    it("should append to a translation if one already exists");
});
