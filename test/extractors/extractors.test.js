describe("jspot.extractors", function() {
    describe(".support", function() {
        describe("the returned function", function() {
            it("should apply defaults before invoking the extractor");
        });

        it("should add the extractor to the registry");
    });

    describe(".get", function() {
        it("should get an extractor using the given extension");
    });

    describe(".get.by_filename", function() {
        it("should get an extractor using the given filename");
    });
});
